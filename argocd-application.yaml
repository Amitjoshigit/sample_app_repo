# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: backstage-app
#   namespace: argocd
# spec:
#   project: default
#   source:
#     repoURL: https://github.com/Amitjoshigit/backstage
#     targetRevision: main
#     path: k8s/base
#   destination:
#     server: https://kubernetes.default.svc
#     namespace: backstage
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
#     syncOptions:
#       - CreateNamespace=true



apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ values.name }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ values.appRepoUrl }}
    targetRevision: main
    path: k8s/base
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ values.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
